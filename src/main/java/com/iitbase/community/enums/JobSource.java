package com.iitbase.community.enums;

public enum JobSource {
    COMMUNITY,            // submitted by IIT community, admin approved
    RECRUITER_EXTERNAL,   // verified recruiter, external apply URL
    RECRUITER_DIRECT      // verified recruiter, apply on IITBase
}