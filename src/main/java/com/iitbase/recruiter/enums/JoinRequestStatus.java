package com.iitbase.recruiter.enums;

public enum JoinRequestStatus {
    PENDING,   // waiting for admin action
    APPROVED,  // admin approved — recruiter profile created
    REJECTED   // admin rejected
}