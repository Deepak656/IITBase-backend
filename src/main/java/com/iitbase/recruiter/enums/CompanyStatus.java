package com.iitbase.recruiter.enums;

public enum CompanyStatus {
    PENDING,    // just created, awaiting IITBase admin review
    VERIFIED,   // approved by IITBase admin
    REJECTED    // rejected by IITBase admin
}