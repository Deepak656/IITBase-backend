package com.iitbase.application.enums;

public enum ApplicationStatus {
    APPLIED,
    SCREENING,
    INTERVIEW,
    OFFER,
    HIRED,
    REJECTED,
    WITHDRAWN    // jobseeker pulls out — recruiter cannot set this
}